export default function taskBlock(trueOrFalse) {
  var task = trueOrFalse;
  var task2 = trueOrFalse;

  if (trueOrFalse) {
    const task = trueOrFalse;
    const task2 = trueOrFalse;
  }

  return [task, task2];
}
